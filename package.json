{
  "name": "sygren-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "SYGREN — Système de Gestion de Relevé Électronique de Note (Monorepo)",
  "scripts": {
    "dev": "cd frontend && bun run dev",
    "dev:backend": "cd backend && bun run dev",
    "lint": "cd frontend && bun run lint",
    "build:frontend": "cd frontend && bun run build",
    "build:backend": "cd backend && go build -o sygren-api main.go",
    "build": "bun run build:backend && bun run build:frontend",
    "migrate:db": "cd backend && go run scripts/migrate_sqlite_to_neon.go"
  }
}
